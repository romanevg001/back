

type UserDTO {
  username: String!
  password: String!
}

type LoginRes {
  id: ID!
  created: String!
  username: String!
  email: String!
  accessToken: String!
}

type Query {
  signIn(username: String!, password: String!): LoginRes! 
  # psrobject(id: ID!): Psrobject!

}
