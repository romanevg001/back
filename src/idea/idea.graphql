type Idea {
  id: ID!
  uodated: String!
  created: String!
  idea: String!
  description: String!
  author: User
  upvotes: Int
  downvotes: Int
  comments: [Comment!]
}


type Query {
  ideas(page: Int, newest: Boolean): [Idea!]
}
